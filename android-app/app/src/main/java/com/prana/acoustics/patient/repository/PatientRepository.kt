package com.prana.acoustics.patient.repository

import com.prana.acoustics.patient.model.Patient

class PatientRepository {
    fun savePatient(patient: Patient): Boolean {
        return true
    }
}